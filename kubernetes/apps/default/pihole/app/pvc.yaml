---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-pvc
  namespace: default
spec:
  storageClassName: nfs-kube
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
